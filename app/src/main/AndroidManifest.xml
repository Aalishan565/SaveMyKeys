<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.savemykeys">

    <application
        android:name=".application.SaveMyKeysApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activities.AddReminderActivity"></activity>
        <activity android:name=".views.activities.AddMemoryActivity" />
        <activity android:name=".views.activities.AddKeyActivity" />
        <activity android:name=".views.activities.HomeActivity" />
        <activity android:name=".views.activities.LoginSignUpActivity" />
        <activity android:name=".views.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>